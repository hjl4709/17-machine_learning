bike 파일의 count 값을 예측하는 ipynb 작성

datetime 칼럼을 연 월 일 시간으로 나눠서 사용하기
판다스의 pd.to_datetime 로 새로운 datetime 칼럼을 만들기
년 월 일 시간으로 새로운 칼럼으로 나누기

기존 datetime, 새로운 datetime, casual, registered 칼럼 버리기

rmsle, rmse 뽑는 함수 만들기

편한된 데이터를 로그를 씌워서 분포를 평평하게 만듬

그래도 만족스럽지 않아서 get_dummies로 더미 데이터를 추가
=> get_dummies를 사용하면 각 열에 알아서 맞게 원 핫 인코딩을 진행해줌

- split을 하기 전에 get_dummies를 해야하는 이유?
  : split을 하고 나서 get_dummies를 하면 칼럼이 달라지기 때문에 predict가 정상적으로 진행되지 않음. (predict는 정확히 같은 이름의 칼럼에서만 진행되기 때문)

```notebook-python
from sklearn.model_selection import cross_val_score
```

를 사용해서 평균 rsme를 구할거임.

```notebook-python
from sklearn.tree import DecisionTreeRegressor
from sklearn.ensemble import GradientBoostingRegressor
from sklearn.ensemble import RandomForestRegressor
from xgboost import XGBRegressor
from lightgbm import LGBMRegressor
from sklearn.linear_model import LinearRegression
```

위 모델들을 사용할 거고 가장 성능이 좋은 모델로 선정해서 best 파라미터 값을 hyperopt로 찾아서 적용시킬예정

이후 bike_test.csv 로 검증을 진행하고

**중간중간에 info와 shape 혹은 그래프를 그려가면서 데이터를 버리거나 선택한 이유를 알 수 있도록 해줘**
